/*import { LoginOptions, handleAuth, handleLogin } from '@auth0/nextjs-auth0';
import { NextRequest } from 'next/server';

const getLoginState = (req: NextRequest, loginOptions: LoginOptions) => {
  return { basket_id: getBasketId(req) };
};

export default handleAuth({
  login: handleLogin({ getLoginState })
});

function getBasketId(req: NextRequest) {
    throw new Error('Function not implemented.');
}

*/